git init инит репозитория
git status статус 
git add .  добавить в отслживание
git commit -a -m"commit" коммит
git checkout -- имя файла  откатить файл на  коммит
git checkout -b имя ветки - создаем навую ветку и на неё переходим
git branch  покажет все ветки и текущую
git branch -v   покажет все ветки и текущую + последние коммиты в них
git branch имя ветки  - создает новую ветки 
git checkout имя ветки - переход на эту ветку